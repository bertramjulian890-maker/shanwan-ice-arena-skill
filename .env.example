# 公开仓库不包含 MCP 服务与飞书密钥。
# 客户端仅需在 OpenClaw / Cursor 等配置里指向 skill.json 中的远程 MCP URL。
#
# 飞书与场馆数据仅在部署侧（如 Render）通过环境变量注入，例如：
# FEISHU_APP_ID、FEISHU_APP_SECRET、FEISHU_BITABLE_APP_TOKEN、FEISHU_BITABLE_TABLE_ID、
# FEISHU_BOOKING_PHONE_FIELD，以及可选的 FEISHU_BOOKING_*_FIELD / *_DEFAULT。
# 详见私有部署仓库中的 `mcp_server/README.md`。
